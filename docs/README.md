# AI Newsletter Frontend

React-based frontend for the AI Newsletter subscription system.

## Quick Start

```bash
npm install
npm run dev
```

## Build for Production

```bash
npm run build
```

## Deployment

This frontend is configured to deploy to GitHub Pages with custom domain `ainews.prajwolsubedi.com.np`.

See GitHub Actions workflow at `.github/workflows/deploy.yml` for automatic deployment.
