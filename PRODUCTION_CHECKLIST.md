# Production Deployment Checklist

## Pre-Deployment

### ✅ Code Cleanup

- [x] Removed empty directories (`mail/`, `pipeline/`)
- [x] Added `.cursor/` to `.gitignore`
- [x] Replaced print statements with logging
- [x] Added proper error handling
- [x] Fixed scheduler time to match 9am Nepal time (3:15am UTC)

### ✅ Configuration Files

- [x] `render.yaml` - Configured for web service and worker
- [x] `requirements.txt` - All dependencies listed
- [x] `Procfile` - Correct start commands
- [x] `.gitignore` - Excludes sensitive files and cache

### ⚠️ Environment Variables Required

Set these in Render dashboard:

**Required:**

- `DATABASE_URL` - Auto-set from PostgreSQL database
- `GEMINI_API_KEY` - Google Gemini API key
- `YOUTUBE_API_KEY` - YouTube Data API key
- `SERP_API_KEY` - SerpAPI key (for transcriptions)
- `SENDER_EMAIL` - Email address for sending newsletters
- `SENDER_PASSWORD` - App password (Gmail App Password recommended)
- `BASE_URL` - Your Render web service URL (e.g., `https://ai-news-web.onrender.com`)
- `CRON_SECRET` - Secret token for protecting internal endpoints
- `SECRET_KEY` - Auto-generated by Render (or set manually)

**Optional (with defaults):**

- `SMTP_SERVER` - Defaults to `smtp.gmail.com`
- `SMTP_PORT` - Defaults to `587`
- `FLASK_ENV` - Set to `production` in render.yaml
- `SCHEDULER_TIMEZONE` - Defaults to `UTC`
- `SCHEDULER_LOG_LEVEL` - Defaults to `INFO`

## Deployment Steps

1. **Push code to GitHub**

   ```bash
   git add .
   git commit -m "Production ready"
   git push origin main
   ```

2. **Create PostgreSQL Database on Render**

   - Dashboard → New → PostgreSQL
   - Name: `ai-news-db`
   - Plan: Free (or upgrade as needed)

3. **Create Web Service**

   - Dashboard → New → Web Service
   - Connect GitHub repository
   - Render will auto-detect `render.yaml`
   - Verify environment variables are set

4. **Configure GitHub Actions**

   - Go to GitHub repository → Settings → Secrets and variables → Actions
   - Add secret: `CRON_SECRET` (same token as Render)
   - Add secret: `RENDER_BASE_URL` (your Render web service URL)
   - Verify workflow files exist:
     - `.github/workflows/trigger-preprocess.yml`
     - `.github/workflows/trigger-pipeline.yml`

5. **Set BASE_URL**

   - In both services, set `BASE_URL` to your web service URL
   - Example: `https://ai-news-web.onrender.com`

6. **Verify Deployment**
   - Check web service is running
   - Check worker service is running
   - Test subscription form
   - Check logs for errors

## Post-Deployment

### Testing Checklist

- [ ] Web service responds at root URL
- [ ] Subscription form works
- [ ] Welcome email is sent on subscription
- [ ] Unsubscribe link works
- [ ] GitHub Actions workflow exists and runs
- [ ] Internal endpoints respond (with correct token)
- [ ] Daily pipeline executes at scheduled times (08:30 NPT and 09:00 NPT)
- [ ] Emails are sent to subscribers
- [ ] Database connections work
- [ ] Error handling works properly

### Monitoring

- Monitor Render logs for errors
- Check database connection health
- Verify scheduled jobs run successfully
- Monitor email sending success rate

## Technical Implementation Notes

### Ephemeral Filesystem Handling

- **Preprocessing** (`preprocess_jobs.py`): Uses in-memory data from `combine_news()` instead of loading from JSON files
- This ensures reliability on Render's free tier where filesystem is ephemeral
- JSON files are still created for debugging but are not required for operation

## Troubleshooting

### Common Issues

1. **Database Connection Errors**

   - Verify `DATABASE_URL` is set correctly
   - Check database is running
   - Verify connection string format

2. **Email Sending Fails**

   - Verify `SENDER_EMAIL` and `SENDER_PASSWORD` are correct
   - For Gmail, use App Password (not regular password)
   - Check SMTP settings

3. **GitHub Actions Not Triggering**

   - Check workflow file exists: `.github/workflows/daily-pipeline.yml`
   - Verify cron schedule is correct
   - Check GitHub Actions are enabled for repository
   - Verify secrets are set correctly
   - Check GitHub Actions logs for errors

4. **API Key Errors**
   - Verify all API keys are set correctly
   - Check API key quotas/limits
   - Verify API keys are active

## Security Notes

- ✅ Secret key is auto-generated by Render
- ✅ Database credentials are managed by Render
- ✅ Environment variables are encrypted
- ⚠️ Never commit `.env` files
- ⚠️ Never commit API keys or passwords
- ⚠️ Use strong SECRET_KEY in production

## Performance Considerations

- Free tier Render services spin down after inactivity
- First request after spin-down may be slow
- Consider upgrading to paid tier for always-on service
- Database free tier has connection limits
